- show_views = false unless defined?(show_views)

%ol.xoxo.post-list
  - posts.each do |post|
    %li{:class => cycle("odd", "even")}
      = link_to_post(post) do
        %span.title= post.title

        - if show_views
          %span.count= post.views.to_i
